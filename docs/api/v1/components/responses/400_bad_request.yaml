description: Bad Request. The request could not be understood or processed due to invalid syntax or state.
content:
  application/json:
    schema:
      $ref: '../schemas/error.yaml'
    example:
      message: "Cannot log time on a closed task."
